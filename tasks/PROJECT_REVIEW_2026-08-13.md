# Project Review — 2026-08-13

Full audit of all 7 labs + planning docs: claimed status vs. on-disk evidence.
Action items extracted to `tasks/todo.md` (the refreshed status board).

## Headline

**The code is healthier than the docs.** All 7 labs are genuinely complete at their
declared scopes. The problems are almost entirely *tracking drift*: docs claiming things
that don't exist (Lab 1 tests), things existing that docs don't claim (Lab 5 Phase 5
code), and leftovers from deleted pipelines masquerading as evidence (Lab 7 M4 media).

**Only one real work item remains in Labs 1–7: Lab 5 Step 5.4** (re-record the pro demo
with the current script and verify no self-collision).

## Per-Lab Verdicts

| Lab | Verdict | Key finding |
|-----|---------|-------------|
| 1 | ✅ Done, docs overclaim | No `tests/` exists, but 3 READMEs claim it. PROJECT-PLAN has 0 checked boxes despite completion. |
| 2 | ✅ Done, code hygiene issues | 34 tests real & matching claims. But `ros2_bridge/` is a byte-identical copy of **Lab 1's 2-link bridge** (wrong robot), and all 12 src scripts carry stale pre-reorg `sys.path` lines. `media/README.md` says folder is empty; it isn't. |
| 3 | ✅ Done, doc gaps only | 34 tests match. `record_lab3_demo.py` + 3 MP4s referenced nowhere. Blog post (success criterion in brief) never written. |
| 4 | ✅ Done, stale metrics | 45 tests. README metrics table predates the slalom redesign (RMS 0.0125 vs validated 0.0027 rad). `slalom_metrics.json` emitted by a deleted script version, contradicts PLAN (clearance 0.0 vs 0.034 m). Blog post never written. |
| 5 | ✅ Done except 5.4 — **TODO was stale in reverse** | Steps 5.1 (SO3-log IK, `record_pro_demo.py:294/:349`) and 5.2 (RRT* + shortcut, `:84`, planned transitions at `:659/:682/:688/:711`) were implemented but never checked off. 5.4 (re-record + no-self-collision verification) genuinely open — existing `pick_place_pro.mp4` can't be confirmed to post-date the fixes. Promised plots missing from `media/`. |
| 6 | ✅ Done, cleanest lab | All M0–M5 evidence present, no orphaned media, docs line-count-exact. Caveat: ~126 unit tests deliberately deleted in restructure (commit `6c6dc86`) — only complete lab with zero automated tests; README documents this honestly. |
| 7 | ✅ Done at M3d scope — misleading leftovers | M4 BLOCKED (position actuators can't track ZMP references; M3e failed 6×; M5 re-scoped to document it — intentional, not bookkeeping). BUT: `media/m4_walking.mp4` + `m4_zmp.png` are outputs of the deleted open-loop pipeline and sit exactly at the paths `tasks/PLAN.md` names as M4 gate artifacts. 5 scratch files (`test_claude.py` etc.) committed in `src/`. `tasks/PLAN.md` 100% unchecked. |

## Planning-Doc Verdicts

- **`plan/MASTER_PLAN.md`** — was ~2 months stale: no status column, timeline showed
  Labs 3–9 as future, repo-structure block described a layout that never existed, Lab 7
  capstone still said "stable walking". **Refreshed 2026-08-13.**
- **`tasks/todo.md`** (root) — last line was "Lab 3 (TBD) — gelecekte eklenecek" with
  Labs 3–7 shipped. **Rewritten as a project status board.**
- **`plan/LAB_06.md` / `LAB_07.md`** — said "Not Started" for complete labs. **Fixed**
  with status + scope-drift notes (welds vs force control; M3d vs walking).
- **`plan/LAB_08.md`** — ⚠️ **critical dependency error, left in place for the Lab 8
  planning session**: Phase 2 says "combine Lab 7's gait generator with the QP" — that
  gait generator does not exist. Lab 8 must build gait itself on the torque/RNEA path
  (which its own architecture already prescribes). MASTER_PLAN now carries this note.
- **`plan/LAB_09.md`** — consistent, but its data pipeline ("demos generated by
  Lab 3–8 controllers") makes Lab 8 the critical path.
- **CLAUDE.md** — Context bullets said "Labs 6–9 planned" while Lab Progress said 6–7
  complete; Lab 5 line said 5.1/5.2/5.4 all open. **Both fixed.**

## Re-scopes that happened silently (now recorded)

1. **Lab 4 capstone**: cluttered-scene trajectory → slalom through 4 obstacles.
2. **Lab 6 M4**: impedance-based internal force control → weld-constraint carry; ±5mm
   separation gate → lift/carry/place distances. Also: unit-test suite → milestone gates.
3. **Lab 7 capstone**: 10+ step walking → standing + weight shift + documented
   infeasibility finding. M5 re-scoped from "demo the walk" to "document the failure".
4. **Docs convention**: `docs/LAB_XX.md` single-file → per-module notes (Labs 1–2) /
   ARCHITECTURE+CODE_WALKTHROUGH (Labs 3–7).
5. **Blog criterion**: quietly dropped for Labs 3–4 (written for 1, 2, 5, 6, 7).

## What was changed in this review (committed)

- `plan/MASTER_PLAN.md` — status column, Lab 8 dependency note, real repo structure,
  timeline, risk register updated (Lab 7 risk marked materialized), platform table.
- `tasks/todo.md` — rewritten as status board + 3-tier doc-hygiene backlog.
- `tasks/lessons.md` — scope note (Labs 1–2 only; later labs use per-lab LESSONS.md).
- `lab-5-grasping-manipulation/tasks/TODO.md` — 5.1/5.2 marked done with code
  evidence; Current Focus → 5.4.
- `plan/LAB_06.md`, `plan/LAB_07.md` — status headers corrected + scope notes.
- `CLAUDE.md` — Context bullets and Lab 5 progress line corrected.
- This report.

**Not changed** (needs code work or user decisions — tracked in `tasks/todo.md` backlog):
Lab 2 bridge/paths, Lab 1 tests claim, Lab 7 media/scratch cleanup, Lab 4 metrics
regeneration, blog-post decision for Labs 3–4, README structure/hero-image gaps,
39 MB GIF, duplicate plan files.
